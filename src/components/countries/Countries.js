import React from 'react'
import "./countries.css"
const Countries = () => {
    return (
        <div className="selectContainer">
            <select className="select">
                <option className="option">İstanbul</option>
            </select>
        </div>
    )
}

export default Countries
